import { defineMessages } from 'react-intl';

export default defineMessages({
  apr: 'APR',

  tooltipText: `Participating in this OmniLend market gives rewards in {token} token. The APR is a representation on how these rewards would be during a 1-year period`,
});
